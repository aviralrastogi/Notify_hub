Django==4.2.16
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.4.0
django-apscheduler==0.7.0
python-dotenv==1.0.1
requests==2.32.3
gunicorn==22.0.0
whitenoise==6.7.0
dj-database-url==2.2.0
# psycopg2-binary is for PostgreSQL on Render (production only)
# It cannot build on Python 3.13 Windows locally — SQLite is used locally instead
# Render will install it via requirements-prod.txt
